#!/usr/bin/env bash

ID="${1:-$(pass ${LOM_PASSWORD_DIRECTORY}all/golds/spreadsheets-id)}"

curl --silent \
  -H "Authorization: Bearer $(cat)" \
  "https://sheets.googleapis.com/v4/spreadsheets/$ID?includeGridData=true"
